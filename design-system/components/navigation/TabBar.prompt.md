Persistent bottom navigation (intentional addition — see readme): Home / Log / Insights / Profile. Frosted-glass background so scroll content is visible underneath.

```jsx
<TabBar items={[{label:'Home',value:'home',icon:'home'},{label:'Log',value:'log',icon:'plus-circle'}]} active="home" onChange={setV} />
```
