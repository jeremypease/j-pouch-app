Small status pill — recovery stage, symptom severity, entry status.

```jsx
<Badge tone="success" icon="check">On track</Badge>
```

Tones: `neutral | success | warning | danger | info`. Muted/warm fills, never saturated alarm colors — this app never punishes.
