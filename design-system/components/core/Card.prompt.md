Base white surface — every card-like block (list rows, dashboard tiles, form sections) wraps this or matches its shadow/radius/border.

```jsx
<Card><strong>Today's log</strong></Card>
```

Flat, soft shadow, 20px radius, 1px warm-gray border. No colored left-border accents.
