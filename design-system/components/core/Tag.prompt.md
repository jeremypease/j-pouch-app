Selectable chip — symptom filters, food/trigger tags on a log entry.

```jsx
<Tag active>Dairy</Tag>
```

Toggle `active` yourself in a filter/multiselect group; not a controlled checkbox, just a styled toggle button.
