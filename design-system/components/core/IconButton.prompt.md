Round icon-only button — back/close chrome, header actions, list-row affordances.

```jsx
<IconButton icon="chevron-left" label="Back" variant="ghost" />
```

Variants: `filled | soft | ghost`. Always pass `label` for accessibility since there's no visible text.
