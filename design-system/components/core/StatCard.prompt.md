Domain-specific dashboard tile (intentional addition — see readme) for a single tracked metric: output count, hydration, etc. Numbers always in monospace.

```jsx
<StatCard label="Output today" value={4} unit="times" icon="activity" trend={-12} />
```
