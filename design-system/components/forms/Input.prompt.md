Text/number field with label, helper text, and error state.

```jsx
<Input label="Dose (mg)" suffix="mg" mono value={2} />
```

Set `mono` for any numeric/dosage/timestamp field so digits align in `--font-mono`.
