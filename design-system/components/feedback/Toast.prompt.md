Transient confirmation — "Entry logged", "Reminder snoozed". Fades/slides in 200ms, auto-dismiss ~3s (wire timing yourself).

```jsx
<Toast tone="success" message="Output logged" onClose={()=>{}} />
```
